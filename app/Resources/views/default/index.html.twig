{% extends 'base.html.twig' %}

{% block content %}
    <h2>{{ 'homepage.title' | trans({}, 'main') }}</h2>
    <div class="col-md-6">
        <div class="service">
            <div class="service__content service__content_graph">
                <h3>{{ 'menu.graph' | trans({}, 'main') }}</h3>
                <ul class="nav nav-pills nav-stacked">
                    <li>
                        <a href="{{ path('graph', {'shift': 1}) }}">{{ 'menu.shift' | trans({'%shift%': 1}, 'main') }}</a>
                    </li>
                    <li>
                        <a href="{{ path('graph', {'shift': 2}) }}">{{ 'menu.shift' | trans({'%shift%': 2}, 'main') }}</a>
                    </li>
                    <li>
                        <a href="{{ path('graph', {'shift': 3}) }}">{{ 'menu.shift' | trans({'%shift%': 3}, 'main') }}</a>
                    </li>
                    <li>
                        <a href="{{ path('graph', {'shift': 4}) }}">{{ 'menu.shift' | trans({'%shift%': 4}, 'main') }}</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>

    <div class="col-md-6">
        <div class="service">
            <a href="">
                <div class="service__content service__content_map"></div>
                <div class="service__popup">
                    <div class="service__title">
                        <p>{{ 'menu.waterponds' | trans({}, 'main') }}</p>
                    </div>
                </div>
            </a>
        </div>
    </div>

    <div class="col-md-6">
        <div class="service">
            <a href="">
                <div class="service__content service__content_docs"></div>
                <div class="service__popup">
                    <div class="service__title">
                        <p>{{ 'menu.docs' | trans({}, 'main') }}</p>
                    </div>
                </div>
            </a>
        </div>
    </div>

{% endblock %}
